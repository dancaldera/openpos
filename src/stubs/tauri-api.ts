/**
 * Web stub for @tauri-apps/api (general namespace)
 *
 * Aliased in vite.web.config.ts. Never actually called in web context.
 */

export const app = {}
export const window = {}
export const event = {}
export const path = {}
